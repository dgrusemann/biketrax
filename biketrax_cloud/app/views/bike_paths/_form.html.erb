<%= form_for(@bike_path) do |f| %>
  <% if @bike_path.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bike_path.errors.count, "error") %> prohibited this bike_path from being saved:</h2>

      <ul>
      <% @bike_path.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :datetime %><br>
    <%= f.datetime_select :datetime %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
